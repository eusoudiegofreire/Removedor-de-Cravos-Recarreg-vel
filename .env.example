# URL do webhook do n8n que recebe as solicitações de entrega da página /curitiba.
# Nunca prefixar com NEXT_PUBLIC_: essa URL só é usada no server action
# (src/app/curitiba/actions.ts) e não deve ficar exposta no bundle do cliente.
N8N_WEBHOOK_URL=
